#include<stdio.h>
#include<math.h>
 int main(){
        float side,area;
        printf("enter the side of square : ");
        scanf("%f",&side);
        area = pow(side,2);
        printf("area of the circle is %f",area);
    }